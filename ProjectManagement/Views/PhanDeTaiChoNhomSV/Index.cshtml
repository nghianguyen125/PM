@using ProjectManagement.Models
@model IEnumerable<ProjectManagement.Models.PhanDeTaiChoNhomSV>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<section class="content-header">
    <h1>Khoa</h1>
</section>
<section class="content">
    <div class="row" style="padding-bottom: 10px;">
        <div class="col-md-12">
            @Html.ActionLink("Tạo mới", "Create", null, new { @class = "btn btn-primary pull-left" })
        </div>
    </div>
    <div class="box">
        <table class="table table-hover table-bordered">
            <tbody>
                <tr>
                    <th style="width: 50px">STT</th>
                    <th>
                        Mã Nhóm
                    </th>
                    <th>
                        Tên Nhóm
                    </th>
                    <th>
                        Đề Tài
                    </th>
                    <th>
                        Số Lượng Thành Viên
                    </th>
                    <th>
                        Ghi Chú
                    </th>
                    <th style="width: 50px; text-align: center">Chức năng</th>
                </tr>
                @{
                    int stt = 0;
                    ProjectManagementEntities db = new ProjectManagementEntities();
                }
                @foreach (var item in Model)
                {
                    stt++;
                    List<PhanDeTaiChoNhomSV> userCreatedBy = db.PhanDeTaiChoNhomSVs.ToList();
                    <tr>
                        <td>@stt</td>
                        <td>
                            @Html.DisplayFor(modelItem => item.NhomSVId)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.NhomSV.TenNhom)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.DeTai.TenDeTai)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.DeTai.SoLuongThanhVien)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.GhiChu)
                        </td>
                        <td align="center">
                            @*@Html.ActionLink("Sửa", "Edit", new {id = item.USER_ID}) |
                                @Html.ActionLink("Xóa", "Delete", new {id = item.USER_ID})*@
                            <ul class="contro_function" style="width: 70px">
                                <li>
                                    <a title="Cập nhật" href="@Url.Action("Edit", new { NId = item.NhomSVId, DTId = item.DeTaiId })">
                                        <i class="fa fa-edit"></i>
                                    </a>
                                </li>
                                <li>|</li>
                                <li>
                                    <a title="Xóa" href="@Url.Action("Delete", new { NId = item.NhomSVId, DTId = item.DeTaiId })">
                                        <i class="fa fa-remove" style="color:red;"></i>
                                    </a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                }
            </tbody>



        </table>
    </div>
</section>