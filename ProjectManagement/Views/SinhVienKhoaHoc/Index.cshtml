@using System.Linq
@using ProjectManagement.App_Start.Classes
@using ProjectManagement.Models
@model IEnumerable<ProjectManagement.Models.SinhVienKhoaHoc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<section class="content-header">
    <h1>Sinh Viên / Khóa Học</h1>
</section>
<section class="content">
    <div class="row" style="padding-bottom: 10px;">
        <div class="col-md-12">
            @Html.ActionLink("Tạo mới", "Create", null, new { @class = "btn btn-primary pull-left" })
        </div>
    </div>
    <div class="box">
        <table class="table table-hover table-bordered">
            <tbody>
                <tr>
                    <th style="width: 50px">STT</th>
                    <th>
                        Mã Sinh viên
                    </th>
                    <th>
                        Tên Sinh viên
                    </th>
                    <th>
                        Tên Ngành
                    </th>
                    <th>
                        Tên Khóa học
                    </th>
                    <th style="width: 50px; text-align: center">Chức năng</th>
                </tr>
                @{
                    int stt = 0;
                    ProjectManagementEntities db = new ProjectManagementEntities();
                }
                @foreach (var item in Model)
                {
                    stt++;
                    List<SinhVienKhoaHoc> userCreatedBy = db.SinhVienKhoaHocs.ToList();
                    <tr>
                        <td>@stt</td>
                        <td>
                            @Html.DisplayFor(modelItem => item.SinhVienId)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.SinhVien.HoTen)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Nganh.TenNganh)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.KhoaHoc.TenKhoaHoc)
                        </td>
                        <td align="center">
                            @*@Html.ActionLink("Sửa", "Edit", new {id = item.USER_ID}) |
                                @Html.ActionLink("Xóa", "Delete", new {id = item.USER_ID})*@
                            <ul class="contro_function" style="width: 70px">
                                <li>
                                    <a title="Cập nhật" href="@Url.Action("Edit", new { SVId = item.SinhVienId, KHId = item.KhoaHocID, NId = item.NganhId, tuNg = item.TuNgay })">
                                        <i class="fa fa-edit"></i>
                                    </a>
                                </li>
                                <li>|</li>
                                <li>
                                    <a title="Xóa" href="@Url.Action("Delete", new { SVId = item.SinhVienId, KHId = item.KhoaHocID, NId = item.NganhId, tuNg = item.TuNgay })">
                                        <i class="fa fa-remove" style="color:red;"></i>
                                    </a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                }
            </tbody>



        </table>
    </div>
</section>
